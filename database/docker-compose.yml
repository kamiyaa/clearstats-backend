name: clearstats

services:
  database:
    image: mysql:latest
    container_name: clearstats-db
    restart: always
    ports:
      - "3306:3306"
    environment:
      - MYSQL_DATABASE=clearstats
#      - MYSQL_USER=...
#      - MYSQL_PASSWORD=...
      - MYSQL_ROOT_PASSWORD=clearstats-pw
